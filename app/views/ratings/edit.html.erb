<!-- Validation failure messages -->

<% if @rating.errors.any? %>
  <% @rating.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>


<!-- Simple full-width example -->

<div class="page-header">
  <h1>Edit Rating #<%= @rating.id %></h1>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_rating/<%= @rating.id %>" class="form-horizontal">
      <!-- Label and input for barber_id -->
      <div class="form-group">
        <label for="barber_id" class="col-sm-2 control-label">
          Barber
        </label>
        <div class="col-sm-10">
          <input type="text" id="barber_id" name="barber_id" placeholder="Barber" class="form-control" value="<%= @rating.barber_id %>">
        </div>
      </div>

      <!-- Label and input for user_id -->
      <div class="form-group">
        <label for="user_id" class="col-sm-2 control-label">
          User
        </label>
        <div class="col-sm-10">
          <input type="text" id="user_id" name="user_id" placeholder="User" class="form-control" value="<%= @rating.user_id %>">
        </div>
      </div>

      <!-- Label and input for rating -->
      <div class="form-group">
        <label for="rating" class="col-sm-2 control-label">
          Rating
        </label>
        <div class="col-sm-10">
          <input type="text" id="rating" name="rating" placeholder="Rating" class="form-control" value="<%= @rating.rating %>">
        </div>
      </div>

      <!-- Label and input for comment -->
      <div class="form-group">
        <label for="comment" class="col-sm-2 control-label">
          Comment
        </label>
        <div class="col-sm-10">
          <textarea id="comment" name="comment" placeholder="Comment" class="form-control" rows="3"><%= @rating.comment %></textarea>
        </div>
      </div>

      <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
          <input type="submit" value="Update Rating" class="btn btn-warning">
          or
          <a href="/ratings/<%= @rating.id %>">Cancel</a>
        </div>
      </div>
    </form>
  </div>
</div>

<!-- End simple full-width example-->

<!-- Responsive panel example -->

<div class="row">
  <div class="col-sm-offset-1 col-sm-10 col-md-offset-2 col-md-8">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Edit Rating #<%= @rating.id %></h3>
      </div>
      <div class="panel-body">
        <form action="/update_rating/<%= @rating.id %>" class="form-horizontal">
          <!-- Label and input for barber_id -->
          <div class="form-group">
            <label for="barber_id" class="col-sm-2 control-label">
              Barber
            </label>
            <div class="col-sm-10">
              <input type="text" id="barber_id" name="barber_id" placeholder="Barber" class="form-control" value="<%= @rating.barber_id %>">
            </div>
          </div>

          <!-- Label and input for user_id -->
          <div class="form-group">
            <label for="user_id" class="col-sm-2 control-label">
              User
            </label>
            <div class="col-sm-10">
              <input type="text" id="user_id" name="user_id" placeholder="User" class="form-control" value="<%= @rating.user_id %>">
            </div>
          </div>

          <!-- Label and input for rating -->
          <div class="form-group">
            <label for="rating" class="col-sm-2 control-label">
              Rating
            </label>
            <div class="col-sm-10">
              <input type="text" id="rating" name="rating" placeholder="Rating" class="form-control" value="<%= @rating.rating %>">
            </div>
          </div>

          <!-- Label and input for comment -->
          <div class="form-group">
            <label for="comment" class="col-sm-2 control-label">
              Comment
            </label>
            <div class="col-sm-10">
              <textarea id="comment" name="comment" placeholder="Comment" class="form-control" rows="3"><%= @rating.comment %></textarea>
            </div>
          </div>

          <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
              <input type="submit" value="Update Rating" class="btn btn-warning">
              or
              <a href="/ratings/<%= @rating.id %>">Cancel</a>
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>


<!-- End responsive panel example -->
