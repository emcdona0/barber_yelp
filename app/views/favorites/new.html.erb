<div class="content-container">
<!-- Validation failure messages -->

<% if @favorite.errors.any? %>
  <% @favorite.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<!-- Simple full-width example-->

<div class="page-header">
  <h1>Add Favorite</h1>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_favorite" class="form-horizontal">
       <!-- Hidden input for current_user.id -->
      <div class="form-group">
      <input type="hidden" placeholder="user_id" name="user_id" class="form-control" value=" <%= current_user.id %>">
      </div>

      <!-- Label and input for barber_id -->
      <div class="form-group">
        <label for="barber_id" class="col-sm-2 control-label">
          Barber
        </label>
        <div class="col-sm-10">
          <%= select_tag(:barber_id, options_from_collection_for_select(Barber.all, :id, :username), :class => "form-control" ) %>

        </div>
      </div>

      <!-- Label and input for barbershop_id -->
      <div class="form-group">
        <label for="barbershop_id" class="col-sm-2 control-label">
          Barbershop
        </label>
        <div class="col-sm-10">
         <%= select_tag(:barbershop_id, options_from_collection_for_select(Barbershop.all, :id, :name), :class => "form-control" ) %>
        </div>
      </div>



      <!-- Label and input for notes -->
      <div class="form-group">
        <label for="notes" class="col-sm-2 control-label">
          Notes
        </label>
        <div class="col-sm-10">
          <textarea id="notes" name="notes" placeholder="Notes" class="form-control" rows="3"><%= @favorite.notes %></textarea>
        </div>
      </div>

      <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
          <input type="submit" value="Create Favorite" class="btn btn-success">
          or
          <a href="/favorites">Cancel</a>
        </div>
      </div>
    </form>
  </div>
</div>

<!-- End simple full-width example-->

<!-- Responsive panel example -->

<div class="row">
  <div class="col-sm-offset-1 col-sm-10 col-md-offset-2 col-md-8">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Add Favorite</h3>
      </div>
      <div class="panel-body">
        <form action="/create_favorite" class="form-horizontal">
          <!-- Hidden input for current_user.id -->
          <div class="form-group">
            <input type="hidden" placeholder="user_id" name="user_id" class="form-control" value=" <%= current_user.id %>">

            <!-- Label and input for barber_id -->
            <div class="form-group">
              <label for="barber_id" class="col-sm-2 control-label">
                Barber
              </label>
              <div class="col-sm-10">
                <%= select_tag(:barber_id, options_from_collection_for_select(Barber.all, :id, :username), :class => "form-control" ) %>
              </div>
            </div>

            <!-- Label and input for barbershop_id -->
            <div class="form-group">
              <label for="barbershop_id" class="col-sm-2 control-label">
                Barbershop
              </label>
              <div class="col-sm-10">
                <%= select_tag(:barbershop_id, options_from_collection_for_select(Barbershop.all, :id, :name), :class => "form-control" ) %>
              </div>
            </div>

            <!-- Label and input for notes -->
            <div class="form-group">
              <label for="notes" class="col-sm-2 control-label">
                Notes
              </label>
              <div class="col-sm-10">
                <textarea id="notes" name="notes" placeholder="Notes" class="form-control" rows="3"><%= @favorite.notes %></textarea>
              </div>
            </div>

            <div class="form-group">
              <div class="col-sm-offset-2 col-sm-10">
                <input type="submit" value="Create Favorite" class="btn btn-success">
                or
                <a href="/favorites">Cancel</a>
              </div>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>

  <!-- End responsive panel example -->
  </div>
